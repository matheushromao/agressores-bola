/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CadastroRequest } from '../../models/cadastro-request';
import { UsuarioResponse } from '../../models/usuario-response';

export interface Cadastrar$Params {
      body: CadastroRequest
}

export function cadastrar(http: HttpClient, rootUrl: string, params: Cadastrar$Params, context?: HttpContext): Observable<StrictHttpResponse<UsuarioResponse>> {
  const rb = new RequestBuilder(rootUrl, cadastrar.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UsuarioResponse>;
    })
  );
}

cadastrar.PATH = '/api/auth/cadastro';
