/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EstatisticaRequest } from '../../models/estatistica-request';
import { EstatisticaResponse } from '../../models/estatistica-response';

export interface RegistrarEstatistica$Params {
  peladaId: number;
  usuarioId: number;
      body: EstatisticaRequest
}

export function registrarEstatistica(http: HttpClient, rootUrl: string, params: RegistrarEstatistica$Params, context?: HttpContext): Observable<StrictHttpResponse<EstatisticaResponse>> {
  const rb = new RequestBuilder(rootUrl, registrarEstatistica.PATH, 'put');
  if (params) {
    rb.path('peladaId', params.peladaId, {});
    rb.path('usuarioId', params.usuarioId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EstatisticaResponse>;
    })
  );
}

registrarEstatistica.PATH = '/api/peladas/{peladaId}/participantes/{usuarioId}/estatistica';
