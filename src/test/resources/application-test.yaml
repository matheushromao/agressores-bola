# Testes: banco separado do de desenvolvimento. O Flyway aplica as migrations
# nele a cada execução, então a suíte também valida os scripts de db/migration.
# Provisório até a Etapa 2 trocar o MySQL local por Testcontainers.
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agressores_test?createDatabaseIfNotExist=true
  jpa:
    show-sql: false
