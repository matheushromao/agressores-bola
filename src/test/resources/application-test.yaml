# Testes: o banco é um MySQL descartável do Testcontainers
# (TestcontainersConfiguration), cuja conexão é injetada via @ServiceConnection.
# O Flyway aplica as migrations nele a cada execução, então a suíte também
# valida os scripts de db/migration.
spring:
  jpa:
    show-sql: false
